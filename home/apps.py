from django.apps import AppConfig

# Configuration for the 'home' app
class HomeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "home"  # App's name used for Django settings and imports
